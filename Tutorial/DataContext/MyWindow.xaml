<Window x:Class="Tutorial.DataContext.MyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="DataContextSample" Height="130" Width="280">

    <StackPanel Margin="15">
        <WrapPanel>
            <TextBlock Text="Window title:  " />
            <!-- TextBox의 Text와 Window의 Title이 결합되는.. -->
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="150" />         
        </WrapPanel>
        <WrapPanel Margin="0,10,0,0">
            <TextBlock Text="Window dimensions: " />
            <TextBox Text="{Binding Width}" Width="50" />           <!-- TextBox의 Text와 Window의 Width가 결합되는.. -->
            <TextBlock Text=" x " />
            <TextBox Text="{Binding Height}" Width="50" />          <!-- TextBox의 Text와 Window의 Height가 결합되는.. -->
        </WrapPanel>
    </StackPanel>

</Window>
